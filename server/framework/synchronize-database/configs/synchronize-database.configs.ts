import { ISynchronizeDatabaseConfig } from '../interfaces';

export const SYNCHRONIZE_DATABASE_CONFIG: ISynchronizeDatabaseConfig[] = [
  { code: 'system', version: 1, action: 'synchronizeSystemModuleDatabase' },
];
